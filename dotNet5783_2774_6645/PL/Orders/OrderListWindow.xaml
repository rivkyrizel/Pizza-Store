<Window x:Class="PL.Orders.OrderListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Orders"
        mc:Ignorable="d"
        Title="OrderListWindow" Height="450" Width="800">
    <Grid>
        <ListView Name="OrderListView" d:ItemsSource="{d:SampleData ItemCount=5}" SelectionChanged="OrderListView_SelectionChanged" MouseDoubleClick="OrderListView_MouseDoubleClick">
            <ListView.View>
                <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="Employee Information" >

                    <GridViewColumn DisplayMemberBinding="{Binding Path=ID}"   Header="ID" Width="100"/>

                    <GridViewColumn DisplayMemberBinding="{Binding Path=CustomerName}" Header="Name" Width="100"/>

                    <GridViewColumn DisplayMemberBinding="{Binding Path=Status}" Header="category" Width="150"/>

                    <GridViewColumn DisplayMemberBinding="{Binding Path=AmountOfItems}" Header="Price" Width="100"/>
                    
                    <GridViewColumn DisplayMemberBinding="{Binding Path=TotalPrice}" Header="Price" Width="100"/>

                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
